apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jenkins
  namespace: default
spec:
  entryPoints:
    - web
  routes:
    - match: PathPrefix(`/jenkins`)
      kind: Rule
      services:
        - name: helm-jenkins
          port: 8080
